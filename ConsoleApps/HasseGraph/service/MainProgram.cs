using System;
using Service.graph;
using System.Collections.Generic;

namespace Service
{
    public class MainProgram
    {
        static public void Main(String[] args)
        {
            // Graph graph = new Graph.Builder()
            //            .addLink("A", "B")
            //            .build();

            // KnowledgeState currentState = new KnowledgeState.Builder()
            //                             .withGraph(graph)
            //                             .withConcept("A")
            //                             .build();

            // LearningSpace space = new LearningSpace(graph, currentState);
            // HashSet<KnowledgeState> learningSpace = space.learningSpace;

            // foreach (KnowledgeState k in learningSpace)
            // {
            //     foreach (string s in k.Concepts)
            //     {
            //         Console.WriteLine(s);
            //     }
            //     Console.WriteLine();
            // }
        }
    }
}
